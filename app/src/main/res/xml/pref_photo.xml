<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="enable_photo"
        android:title="@string/pref_title_enable_photo"/>

    <ListPreference
        android:defaultValue="photo_604"
        android:entries="@array/pref_photo_size_titles"
        android:entryValues="@array/pref_photo_size_values"
        android:key="photo_size"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_photo_size"/>

</PreferenceScreen>
